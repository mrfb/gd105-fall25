void setup() {
  // This block runs once at the start.
  background(255); // paper-white bg
}

void draw() {
  // This block runs every frame.
  line(0, 0, 100, 100);
  line(0, 100, 100, 0);
  line(50, 50, 50, 100);
}
